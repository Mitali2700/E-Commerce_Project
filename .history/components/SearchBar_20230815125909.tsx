import React from 'react'

const SearchBar = () => {
    const handleSearch = () => {}
  return (
    <form className='searchbar'
        onsubmit={handleSearch}>
    </form>
  )
}

export default SearchBar