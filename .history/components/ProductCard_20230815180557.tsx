'use client';
import React from 'react'

const ProductCard = () => {
    const {
        title, description,price, discountPercentage,rating,
        stock,
        brand,
        category,
        thumbnail,
        images,
    }

  return (
    <div>ProductCard</div>
  )
}

export default ProductCard